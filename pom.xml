<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0                              http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <properties>
      <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
      <maven.compiler.source>11</maven.compiler.source>
      <maven.compiler.target>11</maven.compiler.target>
      <failOnMissingWebXml>false</failOnMissingWebXml>
      <project.version>1.0.0</project.version>
    </properties>

    <groupId>io.hotmoka</groupId>
    <artifactId>parent</artifactId>
    <packaging>pom</packaging>
    <version>dev</version>
    <name>Hotmoka and Takamaka Parent</name>

    <modules>
      <module>io-takamaka-code</module>
      <module>io-takamaka-code-constants</module>
      <module>io-takamaka-code-whitelisting</module>
      <module>io-takamaka-code-verification</module>
      <module>io-hotmoka-beans</module>
      <module>io-hotmoka-nodes</module>
      <module>io-hotmoka-network</module>
      <module>io-hotmoka-crypto</module>
      <module>io-hotmoka-patricia</module>
      <module>io-takamaka-code-instrumentation</module>
      <module>io-takamaka-code-engine</module>
      <module>io-takamaka-code-tools</module>
	  <module>io-hotmoka-stores</module>
      <module>io-hotmoka-memory</module>
	  <module>io-hotmoka-xodus</module>
      <module>io-hotmoka-tendermint-dependencies</module>
      <module>io-hotmoka-tendermint</module>
      <module>io-hotmoka-takamaka</module>
	  <module>io-hotmoka-runs</module>
      <module>io-takamaka-examples</module>
      <module>io-takamaka-code-tests</module>      
      <module>hotmoka-and-takamaka-assembly</module>
    </modules>

    <build>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-clean-plugin</artifactId>
		  <version>3.1.0</version>
		  <configuration>
		    <failOnError>false</failOnError>
		    <filesets>
		      <fileset>
		        <directory>modules</directory>
		        <includes>
		          <include>**/*</include>
		        </includes>
		        <followSymlinks>false</followSymlinks>
		      </fileset>
		      <fileset>
		        <directory>docs</directory>
		        <includes>
		          <include>**/*</include>
		        </includes>
		        <followSymlinks>false</followSymlinks>
		      </fileset>
		    </filesets>
		  </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
		  <version>3.8.1</version>
		  <configuration>
		    <release>11</release>
		  </configuration>
        </plugin>
        <plugin>
		  <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-dependency-plugin</artifactId>
          <version>3.1.2</version>
          <executions>
            <execution>
              <id>copy-unnamed-module</id>
              <phase>package</phase>
              <goals>
                <goal>copy-dependencies</goal>
              </goals>
              <configuration>
                <outputDirectory>${project.parent.basedir}/modules/unnamed</outputDirectory>
                <excludeArtifactIds>
                  <!-- we exclude all modules built by Maven: their build will put them at the right place -->
                  io-hotmoka-beans,
                  io-hotmoka-crypto,
                  io-hotmoka-memory,
                  io-hotmoka-nodes,
                  io-hotmoka-patricia,
                  io-hotmoka-network,
                  io-hotmoka-tendermint-dependencies,
                  io-hotmoka-tendermint,
                  io-hotmoka-xodus,
                  io-hotmoka-stores,
                  io-hotmoka-takamaka,
                  io-hotmoka-runs,
                  io-takamaka-code,
                  io-takamaka-code-constants,
                  io-takamaka-code-engine,
                  io-takamaka-code-instrumentation,
                  io-takamaka-code-verification,
                  io-takamaka-code-whitelisting,

                  <!-- we exclude all modules that go into the explicit folder -->
                  it-univr-bcel,
                  gson,
                  slf4j-api

                  <!--  we exclude all modules that go into the automatic folder -->
                  bcel,
                  io-hotmoka-tendermint-dependencies,
                  io-hotmoka-xodus,
                  commons-cli,
                  grpc-stub,
                  protobuf-java,
                  spring-aop,
                  spring-beans,
                  spring-boot,
                  spring-boot-autoconfigure,
                  spring-boot-starter,
                  spring-boot-starter-json,
                  spring-boot-starter-tomcat,
                  spring-boot-starter-web,
                  spring-context,
                  spring-core,
                  spring-expression,
                  spring-jcl,
                  spring-web,
                  spring-webmvc,
                  tomcat-embed-core,
                  tomcat-embed-websocket,

                  <!-- we exclude all modules that are only used for testing -->
                  junit-jupiter,
                  junit-jupiter-api,
                  junit-jupiter-engine,
                  junit-jupiter-params,
                  junit-platform-commons,
                  junit-platform-engine,
                  maven-model,
                  plexus-utils
                </excludeArtifactIds>
              </configuration>
            </execution>
            <execution>
              <id>copy-automatic-modules</id>
              <phase>package</phase>
              <goals>
                <goal>copy-dependencies</goal>
              </goals>
              <configuration>
                <outputDirectory>${project.parent.basedir}/modules/automatic</outputDirectory>
                <includeArtifactIds>
                  bcel,
                  io-hotmoka-tendermint-dependencies,
                  io-hotmoka-xodus,
                  commons-cli,
                  grpc-stub,
                  protobuf-java,
                  spring-aop,
                  spring-beans,
                  spring-boot,
                  spring-boot-autoconfigure,
                  spring-boot-starter,
                  spring-boot-starter-tomcat,
                  spring-boot-starter-web,
                  spring-context,
                  spring-core,
                  spring-expression,
                  spring-jcl,
                  spring-web,
                  spring-webmvc,
                  tomcat-embed-core,
                  tomcat-embed-websocket
                </includeArtifactIds>
              </configuration>
            </execution>
            <execution>
              <id>copy-explicit-modules</id>
              <phase>package</phase>
              <goals>
                <goal>copy-dependencies</goal>
              </goals>
              <configuration>
                <outputDirectory>${project.parent.basedir}/modules/explicit</outputDirectory>
                <includeArtifactIds>
                  it-univr-bcel,
                  gson,
                  slf4j-api
                </includeArtifactIds>
              </configuration>
            </execution>
          </executions>
        </plugin>
		<plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>2.22.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.0.0-M4</version>
          <configuration>
            <includes>
              <include>*.java</include>
            </includes>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-javadoc-plugin</artifactId>
          <version>3.2.0</version>
          <configuration>
            <!-- Default configuration for all reports -->
            <outputDirectory>docs</outputDirectory>
            <finalName>hotmoka-and-takamaka-assembly-${project.version}</finalName>
            <additionalJOptions>
		        <additionalJOption>-Xmaxerrs</additionalJOption>
        		<additionalJOption>65536</additionalJOption>
        		<additionalJOption>-Xmaxwarns</additionalJOption>
        		<additionalJOption>65536</additionalJOption>
      		</additionalJOptions>
          </configuration>
          <executions>
            <execution>
              <id>aggregate</id>
              <goals>
                <goal>aggregate</goal>
              </goals>
              <phase>site</phase>
              <configuration>
                <!-- Specific configuration for the aggregate report -->
              </configuration>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </build>
</project>