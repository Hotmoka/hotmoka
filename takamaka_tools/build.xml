<project name="takamaka_tools" default="dist" basedir=".">
  <description>
    Compiles the takamaka_tools project
  </description>

  <property name="src" location="src"/>
  <property name="build" location="build"/>
  <property name="dist" location="dist"/>

  <target name="clean" description="clean up">
	<delete dir="${build}"/>
	<delete dir="${dist}"/>
  </target>

  <target name="init" depends="clean">
    <mkdir dir="${build}"/>
  </target>

  <target name="compile" depends="init" description="compile the source">
    <javac srcdir="${src}" destdir="${build}">
	  <classpath>
      	<fileset dir="../takamaka_distribution/libs" includes="*.jar" />
       	<fileset dir="../takamaka_distribution/dist" includes="*.jar" />
      </classpath>
    </javac>
  </target>

  <target name="dist" depends="compile" description="generate the distribution">
    <mkdir dir="${dist}"/>
    <jar jarfile="${dist}/takamaka_tools.jar" basedir="${build}" />
  </target>

</project>
