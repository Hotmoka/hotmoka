import {TransactionResponseModel} from "./TransactionResponseModel";

/**
 * The model of a response for a transaction that initializes a node.
 * After that, no more initial transactions can be executed.
 */
export class InitializationTransactionResponseModel extends TransactionResponseModel {

}